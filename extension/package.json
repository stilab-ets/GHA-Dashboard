{
  "name": "gha-dashboard-extension",
  "version": "1.0.0",
  "description": "GHA Dashboard browser extension using Vite and React.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "pack": "npm run build && node src/scripts/assemble.js",
    "dist": "npm run pack && powershell -NoProfile -Command \"Compress-Archive -Path build\\* -DestinationPath gha-dashboard.zip -Force\"",
    "test:unit": "node --test tests/scopeFilters.node.test.mjs tests/trendAnalysis.node.test.mjs",
    "test:unit:browser": "node --test tests/unit",
    "test:e2e": "npm run pack chromium && npx playwright test tests"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "recharts": "^3.5.1",
    "webextension-polyfill": "^0.12.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.1"
  }
}
