# GHA-Dashboard
Dashboard for Github Actions
TODO :
 npm run build
 npm run pack
 Extension assembled into build/ — you can load it as an unpacked extension:
 - In Chrome/Edge: Extensions -> Load unpacked -> select the build/ folder
 - In Firefox: about:debugging -> This Firefox -> Load Temporary Add-on -> select build/manifest.json
